# Webpage to YouTube 자동화 프로그램 설계 문서

---

## 1. 프로젝트 개요

URL 하나를 입력하면 자동으로 유튜브에 4개의 영상이 업로드되는 완전 자동화 파이프라인.

```
입력:   URL 1개 + 포커스 입력 (선택사항)
출력:   유튜브 업로드 4건
          ├─ 5분 영상 (한국어) + 썸네일
          ├─ 5분 영상 (영어)   + 썸네일
          ├─ 쇼츠    (한국어) + 썸네일
          └─ 쇼츠    (영어)   + 썸네일
```

---

## 2. 기술 스택

| 역할 | 유료 (API) | 무료 (로컬) |
|---|---|---|
| 코드 작성 | Claude | — |
| 시나리오 생성 | GPT-4o | Ollama (Llama 3.1 등) |
| 이미지 생성 | DALL-E 3 | Stable Diffusion |
| TTS 음성 | OpenAI TTS | Edge TTS |
| 웹 파싱 | BeautifulSoup + requests | — |
| 언어 감지 | langdetect | — |
| BGM | Pixabay API (무료) | — |
| 영상 합성 | moviepy + ffmpeg | — |
| 유튜브 업로드 | YouTube Data API v3 | — |
| DB | SQLite | — |
| GUI | PyQt or Tkinter | — |
| 패키징 | PyInstaller | — |

> 유료/무료 엔진은 설정 탭에서 선택 가능 (방법 C)

---

## 3. 필요한 API 키 (총 3종)

| API | 발급처 | 난이도 |
|---|---|---|
| OpenAI API Key | platform.openai.com | ⭐ 쉬움 |
| Pixabay API Key | pixabay.com/api | ⭐ 쉬움 |
| Google OAuth2 (YouTube) | Google Cloud Console | ⭐⭐⭐ 복잡 |

---

## 4. 프로그램 구동 방식

### STEP 1 — 웹페이지 수집 `[자동]`

URL과 선택적 포커스 입력을 받아 BeautifulSoup으로 핵심 텍스트를 추출한다.  
langdetect로 한국어/영어를 감지하고, 어떤 언어든 이후 파이프라인은 동일하게 처리된다.  
포커스 입력이 있으면 해당 부분 중심으로, 없으면 전체 페이지에서 적절한 맥락을 자동 판단한다.

---

### STEP 2 — 시나리오 생성 `[자동 → 🔴 컨펌]`

선택한 엔진(GPT-4o or Ollama)으로 5분짜리 후킹 시나리오를 생성한다.

**적용 규칙:**

```
구조         Hook → 문제제기 → 핵심 → 반전 → CTA 5단계 강제
문장         최대 15단어 이하, 짧고 강하게
템포         장면당 2~4초 목표, 빠른 템포 우선
분량         목표 80~100장면, 최소 60장면 이상
문장 1개     = 이미지 1장 (문장 수가 이미지 수 결정)
내레이션     한국어
image_prompt 영어
유튜브 제목  엔진이 자동 생성 (폴더명으로도 사용)
민감한 장면  폭력/참사/실제 인물 피해 → 상징적/간접적 표현으로 자동 우회
이미지 톤    스타일 앵커 이중 적용
```

**자동 검증:**

```
평균 duration > 5초      → 엔진에 재생성 요청
총 장면 수 < 60          → 엔진에 재생성 요청
전체 합계 ≠ 300초 ±10초  → 비율 자동 보정
```

시나리오 + 유튜브 제목 생성 완료 후 **제작자 컨펌 대기**  
OK 시 진행 / 수정 요청 시 재생성

---

### STEP 3 — 쇼츠 시나리오 재구성 `[자동]`

5분 시나리오에서 핵심 장면만 추려 60초 이내 쇼츠 시나리오로 재구성한다.  
쇼츠는 5분 영상으로의 유입을 유도하는 예고편/훅 역할이다.

---

### STEP 4 — 한/영 버전 분기 `[자동]`

내레이션, 자막, 유튜브 메타데이터(제목/설명/태그)를 한/영 각각 생성한다.  
image_prompt는 한/영 공통으로 사용된다.

---

### STEP 5 — 이미지 생성 `[자동]`

선택한 엔진(DALL-E 3 or Stable Diffusion)으로 장면별 이미지를 생성한다.

```
스타일 적용  1차: 시스템 프롬프트에 스타일 규칙 심기 (GPT-4o)
             2차: 코드에서 스타일 앵커 강제 추가 (이중 방어)
스타일 설정  제작자가 자연어로 직접 입력 (기본값 설정됨)
유사 이미지  히스토리 DB 자동 검색
             유사도 80% 이상 → 재사용 여부 팝업
병렬 처리    10개씩 배치 병렬 처리 (DALL-E 3 사용 시)
```

**이미지 스타일 기본값:**

```
clean cartoon illustration style,
minimal and modern design,
black and white line art with selective color accent,
only the most important element highlighted in a single bold accent color,
flat design, simple shapes, sophisticated and elegant,
white background, high contrast, editorial style,
not childish, sleek and refined visual tone
```

---

### STEP 6 — TTS 음성 생성 `[자동]`

선택한 엔진(OpenAI TTS or Edge TTS)으로 한/영 각각 음성을 생성한다.  
실제 음성 길이로 duration_sec를 자동 보정한다.

---

### STEP 7 — BGM 선택 `[자동]`

시나리오 stage 값을 기반으로 Pixabay API에서 자동 선택한다.

```
Hook / 반전   → dramatic, intense
핵심 내용     → energetic, upbeat
CTA           → motivational
```

5분 길이에 맞게 루프 또는 페이드아웃 처리하고, TTS 볼륨 대비 설정된 비율로 덕킹한다.

---

### STEP 8 — 영상 합성 `[자동]`

moviepy + ffmpeg로 4개 영상을 합성한다.

```
5분 영상 (한국어)  16:9  이미지 + TTS(한) + BGM + 자막(한)
5분 영상 (영어)    16:9  이미지 + TTS(영) + BGM + 자막(영)
쇼츠    (한국어)   9:16  상단 제목(한) + 이미지 원본 + 하단 여백
쇼츠    (영어)     9:16  상단 제목(영) + 이미지 원본 + 하단 여백
```

전환 방식: 전 구간 단순 CUT / 전환 시간 0초 / 특수효과 없음  
FPS: 코드 내부 24fps 고정 (스틸 이미지 컷 전환이라 설정 불필요)

---

### STEP 9 — 썸네일 생성 `[자동]`

엔진이 썸네일 전용 프롬프트를 자동 생성하고 DALL-E 3으로 이미지를 만든다.  
Pillow로 한/영 텍스트를 오버레이한다.

**썸네일 전략 (GPT-4o 시스템 프롬프트 고정):**

```
구도         인물/오브젝트는 좌측 or 우측 배치, 텍스트 공간 확보
             클로즈업 or 극적인 앵글 우선
감정 유발    충격 / 궁금증 / 공포 / 흥미 중 하나 반드시 포함
텍스트       핵심 키워드 1~3단어 압축, 큰 폰트, 고대비
             숫자 포함 시 CTR 상승 (예: 40%, 3가지, 1000년)
금지         복잡한 배경 / 텍스트 과다 / 밋밋한 구도
스타일 참고  MrBeast / 지식인사이드 / 사피엔스 스튜디오
```

썸네일 총 4종 생성:

```
5분 영상 썸네일 (한국어)   이미지 베이스 공통
5분 영상 썸네일 (영어)     텍스트만 한/영 교체
쇼츠 썸네일    (한국어)
쇼츠 썸네일    (영어)
```

---

### STEP 10 — 결과물 저장 `[자동]`

```
[설정된 경로]/[유튜브 제목]/
  ├─ scenes/
  ├─ audio/  (ko / en)
  ├─ thumbnails/
  └─ videos/
```

---

### STEP 11 — 유튜브 업로드 `[자동 → 🔴 컨펌]`

4개 영상 + 썸네일 + 메타데이터 최종 확인 후 업로드한다.

```
한국어 채널 → 5분 영상 + 쇼츠
영어 채널   → 5분 영상 + 쇼츠
```

**제작자 컨펌 대기** — OK 시 4건 업로드 실행 / 취소 시 중단

---

## 5. Scene JSON 구조

```json
{
  "scene_id": 1,
  "stage": "hook",
  "narration": "2030년, 직업의 40%가 사라집니다.",
  "duration_sec": 3,
  "image_prompt": "year 2030 with disappearing job titles, bold graphic, white background",
  "text_overlay": "2030년, 40% 소멸"
}
```

---

## 6. 비용 구조

### 영상 1편 제작 예상 비용 (유료 API 사용 시)

```
GPT-4o  시나리오/번역/썸네일    약 $0.10 ~ 0.20   (5%)
DALL-E 3 이미지 80~100장        약 $3.20 ~ 4.00   (90%)
OpenAI TTS 한/영 5분 분량       약 $0.05 ~ 0.10   (5%)
Pixabay BGM                     무료
─────────────────────────────────────────────────
총합                             약 $3.35 ~ 4.30
                                 (약 4,500 ~ 5,800원)
```

### 비용 절감 포인트

```
유사 이미지 재사용   → DALL-E 3 비용 직접 절감 (가장 효과적)
로컬 엔진 사용       → 전기세만 발생
이미지 품질 HD→표준  → 장당 $0.04 → $0.02
```

### 테스트 전략

```
1단계  전부 로컬/무료 툴로 파이프라인 흐름 검증 (비용 0원)
2단계  같은 URL로 무료 vs 유료 품질 비교
3단계  비교 결과 보고 혼합 비율 결정
```

---

## 7. UX/UI 구조

### 탭 구성

```
┌──────┬──────┬────────┬──────┬──────┬──────┐
│ 메인  │ 결과  │ 히스토리 │ 스케줄 │ 통계  │ 설정  │
└──────┴──────┴────────┴──────┴──────┴──────┘
```

### 메인 탭

```
URL 입력창
포커스 입력 (선택사항) — "이 부분을 중심으로..."
[시작] 버튼

진행 상황 프로그레스바  STEP 3/11 이미지 생성 중 40%

🔴 컨펌 영역 (시나리오 미리보기 + OK / 재생성)

💰 현재 예상 비용 표시
```

### 결과 탭

```
영상 미리보기 4종 (5분 한/영, 쇼츠 한/영)
썸네일 미리보기 4종
저장 경로 표시
🔴 최종 업로드 컨펌 (업로드 / 취소)
```

### 히스토리 탭

```
검색창 + 필터
영상별 카드 (제목 / 날짜 / 이미지 장수 / 비용 / 업로드 상태)
[재활용] [이미지보기] [삭제]
누적 총 비용 표시
```

### 스케줄 탭

```
업로드 큐 목록
반복 업로드 설정 (없음 / 매일 / 월수금 / 직접설정)
업로드 시간 설정
```

### 통계 탭

```
월별 비용 현황
영상별 성과 (조회수 / CTR / 구독 증가)
이미지 재사용으로 절약한 비용
```

### 설정 탭

```
1. 저장 경로          폴더 선택 버튼
2. 유튜브 채널        채널 ID / 공개 설정 / 예약 업로드 / 카테고리
3. 영상 품질          해상도 (1080p/720p/480p) / 비트레이트
4. 시나리오 & 템포    영상 길이 / 장면당 목표 시간 / 톤 / 후킹 강도
5. 이미지 스타일      자연어 입력창 (기본값 설정됨)
6. TTS 음성           속도 / 한국어 성우 / 영어 성우
7. BGM                볼륨 비율 슬라이더 (10%~40%)
8. 자막               ON/OFF / 폰트 크기 / 위치 / 색상
9. 엔진 선택          시나리오: GPT-4o ↔ Ollama
                       이미지: DALL-E 3 ↔ Stable Diffusion
                       TTS: OpenAI ↔ Edge TTS
10. API 키            엔진 선택 시 동적으로 입력창 표시
                       OpenAI Key (GPT-4o / DALL-E 3 / TTS 공유)
                       Pixabay Key (항상 표시)
                       Google OAuth2 (항상 표시 + 인증 버튼)

[기본값으로 초기화]  [저장]  (config.py 자동 반영)
```

---

## 8. 컨펌 구조

```
STEP 2   시나리오 생성 완료  → 🔴 컨펌 대기
STEP 11  업로드 직전         → 🔴 컨펌 대기
나머지   전부 자동 진행
```

---

## 9. 추가 기능

| 기능 | 설명 |
|---|---|
| 체크포인트 | 각 스텝 완료 시 중간 결과 저장, 실패 시 해당 스텝부터 재시작 |
| 유사 이미지 재사용 | 히스토리 DB에서 유사도 80% 이상 이미지 재사용 팝업 |
| 히스토리 관리 | 제목/날짜/비용/업로드 상태 기록, 재활용 가능 |
| 비용 추적 | 실시간 예상 비용 + 월별 누적 비용 표시 |
| 업로드 스케줄러 | 큐에 쌓아두고 예약 업로드 |
| 유튜브 성과 트래킹 | 조회수 / CTR / 구독 증가 확인 |
| 이미지 부분 재생성 | 마음에 안 드는 장면만 선택해서 재생성 |
| 채널 확장 | 한/영 외 언어 채널 추가 가능 |

---

## 10. 폴더 트리

```
webpage-to-youtube/                          # 최상위 폴더
│
├── webpage-to-youtube-설계문서.md            # 설계 문서 + 진행 상황 추적
│
└── src/                                     # 모든 코딩 자료
    │
    ├── main.py
    ├── config.py
    ├── requirements.txt
    ├── .env
    ├── .gitignore
    ├── webpage-to-youtube.spec              # PyInstaller 설정
    ├── build.py                             # 빌드 자동화 스크립트
    │
    ├── core/
    │   ├── pipeline.py                      # 전체 스텝 순서 관리
    │   ├── checkpoint.py                    # 체크포인트 저장/복구
    │   └── cost_tracker.py                  # 실시간 비용 계산
    │
    ├── scraper/
    │   ├── fetcher.py                       # URL 요청 + HTML 수집
    │   ├── parser.py                        # 핵심 텍스트 추출
    │   └── language_detector.py             # 한/영 언어 감지
    │
    ├── scenario/
    │   ├── generator/
    │   │   ├── gpt_generator.py             # GPT-4o 시나리오 생성
    │   │   └── ollama_generator.py          # Ollama 시나리오 생성
    │   ├── validator.py                     # 시나리오 자동 검증
    │   ├── shorts_builder.py                # 쇼츠 시나리오 재구성
    │   └── translator.py                    # 한/영 분기 + 번역
    │
    ├── image/
    │   ├── generator/
    │   │   ├── dalle_generator.py           # DALL-E 3 이미지 생성
    │   │   └── sd_generator.py              # Stable Diffusion 생성
    │   ├── batch_processor.py               # 10개씩 배치 병렬 처리
    │   ├── style_anchor.py                  # 스타일 앵커 적용
    │   └── cache_matcher.py                 # 유사 이미지 검색/재사용
    │
    ├── tts/
    │   ├── openai_tts.py                    # OpenAI TTS 음성 생성
    │   ├── edge_tts.py                      # Edge TTS 음성 생성
    │   └── duration_corrector.py            # 실제 음성 길이 보정
    │
    ├── bgm/
    │   ├── pixabay_fetcher.py               # Pixabay BGM 검색/다운로드
    │   ├── stage_selector.py                # stage별 BGM 자동 선택
    │   └── audio_mixer.py                   # 볼륨 덕킹 처리
    │
    ├── video/
    │   ├── landscape_composer.py            # 5분 영상 합성 (16:9)
    │   ├── shorts_composer.py               # 쇼츠 합성 (9:16 레터박스)
    │   ├── subtitle_renderer.py             # 자막 렌더링
    │   └── encoder.py                       # ffmpeg 인코딩 설정
    │
    ├── thumbnail/
    │   ├── prompt_generator.py              # 썸네일 프롬프트 생성
    │   ├── image_generator.py               # DALL-E 3 썸네일 이미지
    │   └── text_overlay.py                  # Pillow 텍스트 오버레이
    │
    ├── uploader/
    │   ├── youtube_uploader.py              # YouTube API 업로드
    │   ├── oauth_handler.py                 # Google OAuth2 인증
    │   └── metadata_builder.py              # 제목/설명/태그 구성
    │
    ├── scheduler/
    │   ├── upload_queue.py                  # 업로드 큐 관리
    │   └── cron_runner.py                   # 예약 실행
    │
    ├── history/
    │   ├── history_manager.py               # 히스토리 저장/조회
    │   └── image_db.py                      # 이미지 캐시 DB 관리
    │
    ├── analytics/
    │   ├── youtube_analytics.py             # 유튜브 성과 트래킹
    │   └── cost_reporter.py                 # 비용 리포트
    │
    ├── gui/
    │   ├── app.py                           # GUI 앱 진입점
    │   ├── tabs/
    │   │   ├── main_tab.py
    │   │   ├── result_tab.py
    │   │   ├── history_tab.py
    │   │   ├── schedule_tab.py
    │   │   ├── analytics_tab.py
    │   │   └── settings_tab.py
    │   └── components/
    │       ├── confirm_dialog.py            # 컨펌 팝업
    │       ├── progress_bar.py              # 진행 상황 바
    │       ├── video_preview.py             # 영상 미리보기
    │       ├── cost_display.py              # 비용 표시
    │       └── engine_selector.py           # 엔진 선택 + API 키 입력
    │
    ├── prompts/
    │   ├── scenario_system.txt              # 시나리오 생성 규칙
    │   ├── scenario_shorts_system.txt       # 쇼츠 재구성 규칙
    │   ├── image_style_anchor.txt           # 스타일 앵커 기본값
    │   └── thumbnail_system.txt             # 썸네일 전략 규칙
    │
    ├── assets/
    │   ├── fonts/
    │   │   ├── korean_bold.ttf
    │   │   └── english_bold.ttf
    │   └── bgm/                             # BGM 캐시
    │
    ├── database/
    │   ├── history.db                       # SQLite 히스토리 DB
    │   └── image_cache.db                   # 이미지 프롬프트 + 경로 DB
    │
    ├── build/                               # PyInstaller 빌드 임시 파일
    ├── dist/                                # 최종 실행 파일 출력
    │   ├── webpage-to-youtube.exe           # Windows
    │   └── webpage-to-youtube.app           # Mac
    │
    └── output/
        └── [유튜브 제목]/
            ├── scenes/
            ├── audio/
            │   ├── ko/
            │   └── en/
            ├── thumbnails/
            │   ├── thumb_landscape_ko.jpg
            │   ├── thumb_landscape_en.jpg
            │   ├── thumb_shorts_ko.jpg
            │   └── thumb_shorts_en.jpg
            └── videos/
                ├── landscape_ko.mp4
                ├── landscape_en.mp4
                ├── shorts_ko.mp4
                └── shorts_en.mp4
```

---

## 11. 버그 수정 가이드

파일이 기능별로 세분화되어 있어 해당 파일만 떼어서 수정 가능하다.

```
이미지 생성 버그     → src/image/generator/dalle_generator.py
자막 버그            → src/video/subtitle_renderer.py
유사 이미지 버그     → src/image/cache_matcher.py
업로드 버그          → src/uploader/youtube_uploader.py
시나리오 검증 버그   → src/scenario/validator.py
BGM 선택 버그        → src/bgm/stage_selector.py
비용 계산 버그       → src/core/cost_tracker.py
GUI 특정 탭 버그     → src/gui/tabs/해당탭.py
```

---

## 12. 코딩 진행 상황

> **[Claude에게 지시]**
> 이 MD 파일은 코딩 진행 상황을 추적하는 문서다.
> 아래 규칙을 반드시 따를 것:
>
> 1. 파일 하나 코딩 완료 시 → 해당 항목 [ ] 를 [x] 로 즉시 변경
> 2. 대화 단위 작업 전부 완료 시 → "진행 현황 요약"의 완료 수, 마지막 작업, 다음 작업 업데이트
> 3. MD 파일 업데이트는 코드 파일 저장 직후 반드시 실행
> 4. 사용자가 요청하지 않아도 자동으로 업데이트할 것
> 5. 작업 중 설계가 변경되면 관련 섹션도 함께 수정할 것
> 6. **각 대화 종료 시 해당 대화에서 작성된 모든 파일을 버그 체크하고 결과를 "버그 체크 결과" 섹션에 기록할 것**
>    - 심각도: 🔴 치명 (런타임 오류) / 🟡 경고 (잠재적 오류) / 🔵 설계 주의사항
>    - 수정한 버그는 [x], 다음 대화에서 수정할 버그는 [ ]
>    - 수정 시 파일명:라인수도 함께 기록

### 진행 현황 요약
```
전체 진행률: 12 / 12 단계 완료 ✅
마지막 작업: 대화 12 — 통합 테스트 (import 검증 40/40, 기능 테스트, 6건 치명적 버그 수정)
```

### 단계별 체크리스트

**대화 1 — 기반 구조**
- [x] `config.py` — 전역 설정 + 기본값
- [x] `main.py` — 파이프라인 실행 진입점
- [x] `core/pipeline.py` — 전체 스텝 순서 관리
- [x] `core/checkpoint.py` — 체크포인트 저장/복구
- [x] `core/cost_tracker.py` — 실시간 비용 계산

**대화 2 — 웹 스크래퍼**
- [x] `scraper/fetcher.py` — URL 요청 + HTML 수집
- [x] `scraper/parser.py` — 핵심 텍스트 추출
- [x] `scraper/language_detector.py` — 한/영 언어 감지

**대화 3 — 시나리오**
- [x] `scenario/generator/gpt_generator.py` — GPT-4o 시나리오 생성
- [x] `scenario/generator/ollama_generator.py` — Ollama 시나리오 생성
- [x] `scenario/validator.py` — 시나리오 자동 검증
- [x] `scenario/shorts_builder.py` — 쇼츠 시나리오 재구성
- [x] `scenario/translator.py` — 한/영 분기 + 번역
- [x] `prompts/scenario_system.txt` — 시나리오 생성 시스템 프롬프트 (추가)
- [x] `prompts/scenario_shorts_system.txt` — 쇼츠 재구성 프롬프트 (추가)

**대화 4 — 이미지 생성**
- [x] `image/generator/dalle_generator.py` — DALL-E 3 이미지 생성
- [x] `image/generator/sd_generator.py` — Stable Diffusion 생성
- [x] `image/batch_processor.py` — 배치 병렬 처리
- [x] `image/style_anchor.py` — 스타일 앵커 적용
- [x] `image/cache_matcher.py` — 유사 이미지 검색/재사용
- [x] `prompts/image_style_anchor.txt` — 스타일 앵커 기본값 (추가)

**대화 5 — TTS + BGM**
- [x] `tts/openai_tts.py` — OpenAI TTS
- [x] `tts/edge_tts.py` — Edge TTS
- [x] `tts/duration_corrector.py` — 음성 길이 보정
- [x] `bgm/pixabay_fetcher.py` — Pixabay BGM 검색/다운로드
- [x] `bgm/stage_selector.py` — stage별 BGM 자동 선택
- [x] `bgm/audio_mixer.py` — 볼륨 덕킹 처리

**대화 6 — 영상 합성**
- [x] `video/landscape_composer.py` — 5분 영상 합성 (16:9)
- [x] `video/shorts_composer.py` — 쇼츠 합성 (9:16)
- [x] `video/subtitle_renderer.py` — 자막 렌더링
- [x] `video/encoder.py` — ffmpeg 인코딩 설정

**대화 7 — 썸네일**
- [x] `thumbnail/prompt_generator.py` — 썸네일 프롬프트 생성
- [x] `thumbnail/image_generator.py` — DALL-E 3 썸네일
- [x] `thumbnail/text_overlay.py` — Pillow 텍스트 오버레이
- [x] `prompts/thumbnail_system.txt` — 썸네일 전략 시스템 프롬프트 (추가)

**대화 8 — 유튜브 업로더**
- [x] `uploader/youtube_uploader.py` — YouTube API 업로드
- [x] `uploader/oauth_handler.py` — Google OAuth2 인증
- [x] `uploader/metadata_builder.py` — 제목/설명/태그 구성

**대화 9 — 부가 기능**
- [x] `scheduler/upload_queue.py` — 업로드 큐 관리
- [x] `scheduler/cron_runner.py` — 예약 실행
- [x] `history/history_manager.py` — 히스토리 저장/조회
- [x] `history/image_db.py` — 이미지 캐시 DB
- [x] `analytics/youtube_analytics.py` — 유튜브 성과 트래킹
- [x] `analytics/cost_reporter.py` — 비용 리포트

**대화 10 — GUI**
- [x] `gui/app.py` — GUI 앱 진입점
- [x] `gui/tabs/main_tab.py`
- [x] `gui/tabs/result_tab.py`
- [x] `gui/tabs/history_tab.py`
- [x] `gui/tabs/schedule_tab.py`
- [x] `gui/tabs/analytics_tab.py`
- [x] `gui/tabs/settings_tab.py`
- [x] `gui/components/confirm_dialog.py`
- [x] `gui/components/progress_bar.py`
- [x] `gui/components/video_preview.py`
- [x] `gui/components/cost_display.py`
- [x] `gui/components/engine_selector.py`

**대화 11 — 패키징**
- [x] `webpage-to-youtube.spec` — PyInstaller 설정
- [x] `build.py` — 빌드 자동화 스크립트
- [ ] Windows .exe 빌드 테스트
- [ ] Mac .app 빌드 테스트

**대화 12 — 통합 테스트**
- [x] 전체 import 검증 (40/40 모듈 OK)
- [x] STEP 1 웹 수집 기능 테스트 (Wikipedia → 8014자 추출, 언어 감지 OK)
- [x] STEP 3 쇼츠 빌더 테스트 (5장면 → 3장면 압축 OK)
- [x] Edge TTS 음성 생성 테스트 (영어 2장면 → mp3 생성 OK)
- [x] Duration Corrector 테스트 (파일크기 추정 fallback OK — ffmpeg 설치 시 정확도 향상)
- [x] History Manager CRUD 테스트 (add/search/delete OK)
- [x] Upload Queue CRUD 테스트 (enqueue/cancel/remove OK)
- [x] CronRunner 스케줄 설정 테스트 (daily/next_upload_time OK)
- [x] Cost Reporter 비용 요약 테스트 (format_summary_text OK)
- [x] pipeline.py import 불일치 6건 수정 완료
- [ ] 유료 엔진(GPT-4o/DALL-E 3) 전체 파이프라인 E2E 테스트 (API 키 필요)
- [ ] 유튜브 업로드 E2E 테스트 (OAuth2 인증 필요)
- [ ] 영상 합성 테스트 (ffmpeg 설치 필요)

---

### 버그 체크 결과

> 심각도: 🔴 치명 (런타임 오류) / 🟡 경고 (잠재적 오류) / 🔵 설계 주의사항

---

**대화 2 — 웹 스크래퍼** ✅ 체크 완료

- [x] 🔵 BUG-05 `scraper/language_detector.py` — `LangDetectException` import 후 미사용 → import 제거로 수정
- [x] 🟡 BUG-06 `scraper/language_detector.py:76` — langdetect seed 미설정 → 대화 3에서 `DetectorFactory.seed = 0` 추가 완료
- [ ] 🔵 BUG-07 `scraper/fetcher.py` — 호출마다 새 세션 생성 (단일 URL 파이프라인이므로 무영향, 향후 다중 요청 시 연결 풀 미활용)
- [ ] 🔵 BUG-08 `scraper/parser.py` — `<div>` 태그 텍스트 미수집 → 의도적 제외. 일부 현대 사이트에서 콘텐츠 누락 가능 (대화 12 통합 테스트 후 보완)

---

**대화 3 — 시나리오** ✅ 체크 완료

- [x] 🔵 BUG-09 `scenario/generator/gpt_generator.py:22` — `_SCENE_REQUIRED` 미사용 변수 선언 → 즉시 제거
- [x] 🟡 BUG-10 `scenario/generator/gpt_generator.py:61` — `max_tokens=8000`이 100장면 생성 시 응답 잘림 위험 → 12000으로 상향
- [x] 🔵 BUG-11 `scenario/translator.py:217` — GPT가 제목을 따옴표로 감싸 반환하는 경우 미처리 → `.strip("\"'")` 추가
- [ ] 🟡 BUG-12 `scenario/generator/gpt_generator.py:62` — OpenAI 네트워크 오류 시 MAX_RETRIES를 전부 소진 후 실패. 연결 오류는 즉시 break가 더 적절 (대화 12 통합 테스트 후 개선 예정)
- [ ] 🔵 BUG-13 `scenario/validator.py:55` — duration 비율 보정 후 클램핑으로 인해 총합이 정확히 target에 맞지 않을 수 있음 (±10초 허용 범위 내이므로 허용)
- [ ] 🔵 BUG-14 `scenario/translator.py:179` — `response_format=json_object`이면 list 직접 반환 불가. 해당 분기는 실행되지 않는 방어 코드 (무해)

---

**대화 5 — TTS + BGM** ✅ 체크 완료

- [x] 🟡 BUG-19 `tts/edge_tts.py:58` — PyQt6 GUI 환경에서 이미 실행 중인 이벤트 루프에 `asyncio.run()` 호출 시 `RuntimeError` 발생 → `_run_async()` 헬퍼 추가 (실행 중 루프 감지 → ThreadPoolExecutor로 우회) 즉시 수정
- [ ] 🟡 BUG-20 `tts/openai_tts.py:38` — `OPENAI_API_KEY` 빈 문자열일 때 사전 검증 없이 루프 진입, 100개 씬 전부 API 오류 후 빈 파일 생성 → 대화 12 통합 테스트 시 API 키 검증 로직 추가 예정
- [ ] 🔵 BUG-21 `bgm/pixabay_fetcher.py:18` — Pixabay Music API 엔드포인트 `https://pixabay.com/api/music/` 및 응답 필드명은 통합 테스트 시 실제 API로 검증 필요. 실패 시 무음 BGM으로 graceful 대체됨
- [ ] 🔵 BUG-22 `tts/duration_corrector.py:35` — `len(scenes) > len(audio_paths)`인 경우 초과 씬은 duration 미보정 (원본 duration_sec 유지). TTS 생성이 정상이면 발생하지 않으나, 시나리오-오디오 길이 불일치 시 영상 타이밍 오차 가능

---

**대화 6 — 영상 합성** ✅ 체크 완료

- [x] 🔴 BUG-04 `video/shorts_composer.py` — BUG-04(pipeline.py) 해결: scene_id 기반으로 landscape audio_paths에서 해당 오디오만 필터링하는 `_filter_audio_by_scene_ids()` 구현 완료. scene_id 범위 초과 시 경고 로그 + 빈 문자열 처리.
- [ ] 🟡 BUG-23 `video/landscape_composer.py:57` — `image_paths`가 `scenes`보다 적을 때 마지막 이미지를 재사용. 대부분 batch_processor 정상 동작 시 발생하지 않으나, 이미지 생성 일부 실패 시 화면 반복 발생 가능. (대화 12 통합 테스트 후 보완)
- [ ] 🟡 BUG-24 `video/subtitle_renderer.py:38` — `ImageFont.load_default()`는 PIL 버전에 따라 size 인자를 지원하지 않을 수 있음. 한국어 TTF 폰트 파일 배포 필수. 폰트 미배포 시 한글 자막 깨짐.
- [ ] 🔵 BUG-25 `video/encoder.py:65` — `threads=2` 고정. 시스템 CPU 코어 수에 맞게 동적 설정하면 인코딩 속도 향상 가능. 현재 다중 영상 동시 인코딩 고려해 보수적으로 설정.
- [ ] 🔵 BUG-26 `video/shorts_composer.py:_filter_audio_by_scene_ids` — shorts 시나리오 scene_id가 0-based로 생성될 경우 인덱스 off-by-one 발생. `shorts_builder.py`가 1-based scene_id를 유지하는지 통합 테스트에서 확인 필요.

---

**대화 7 — 썸네일** ✅ 체크 완료

- [ ] 🔵 BUG-27 `thumbnail/prompt_generator.py:_fallback_result` — `context: str` 파라미터 선언 후 미사용. 런타임 오류 없음, context-aware fallback 구현 시 활용 예정
- [ ] 🟡 BUG-28 `thumbnail/image_generator.py:79` — DALL-E 3 프롬프트 최대 4000자 제한. `_STYLE_SUFFIX`(약 170자) 합산해도 일반 사용 시 문제없으나, 매우 긴 프롬프트 입력 시 API 오류 가능. 통합 테스트 시 트리밍 로직 추가 예정
- [ ] 🔵 BUG-29 `thumbnail/text_overlay.py:_load_font` — `ImageFont.load_default(size=...)` Pillow 10.0.0+ 전용. 하위 버전 시 TypeError → size 없는 기본 폰트로 폴백 처리됨 (BUG-24와 동일 이슈, try/except 완비)

---

**대화 8 — 유튜브 업로더** ✅ 체크 완료

- [ ] 🟡 BUG-30 `uploader/youtube_uploader.py:_upload_file` — resumable upload session 서버 만료(6시간) 시 기존 request 재사용 불가. 장시간 업로드 또는 네트워크 단절 복구 시 session 재생성 로직 필요 (대화 12 통합 테스트 후 개선)
- [ ] 🔵 BUG-31 `uploader/youtube_uploader.py:_set_thumbnail` — Verified 채널이 아니면 thumbnails.set이 HTTP 403 반환. 경고 로그로 graceful 처리되어 업로드 자체는 정상 완료 (무해)
- [ ] 🔵 BUG-32 `uploader/oauth_handler.py:get_authenticated_service` — `run_local_server(port=0)`는 브라우저 없는 환경(서버/headless)에서 실패. 현재 GUI 전용 설계이므로 범위 내. 향후 headless 지원 시 `run_console()` 분기 추가 필요
- [ ] 🔵 BUG-33 `uploader/metadata_builder.py:_build_tags` — YouTube 태그는 개수 기준이 아닌 모든 태그 문자 합산 500자 제한. `tags[:30]`으로 약 240자 이내 유지되어 안전 범위. 매우 긴 제목어 포함 시 초과 가능성 있으나 실사용 시 발생 확률 낮음

---

**대화 4 — 이미지 생성** ✅ 체크 완료

- [x] 🟡 BUG-15 `image/generator/dalle_generator.py:73` — `b64_json` 응답이 None일 경우 `base64.b64decode(None)` TypeError → `if not b64_data: raise ValueError(...)` 추가로 즉시 수정
- [ ] 🔵 BUG-16 `image/style_anchor.py:28` — `_cached_anchor` 전역 변수가 병렬 스레드에서 동시 접근 가능. 동일값 할당이라 실질적 크래시 없음 (무해), 향후 threading.Lock 적용 고려
- [ ] 🔵 BUG-17 `image/batch_processor.py:80` — `reuse_callback=None`이면 유사 이미지 발견해도 항상 새로 생성. CLI 모드 자동 진행을 위한 설계 의도적 동작. GUI에서 콜백 반드시 전달 필요
- [ ] 🔵 BUG-18 `image/cache_matcher.py` — Jaccard 유사도는 단어 순서를 고려하지 않아 의미가 다른 프롬프트도 유사하다고 판정할 수 있음. 실사용 후 오탐률 확인 필요 (대화 12 통합 테스트 후 보완)

---

**대화 1 — 기반 구조** ✅ 체크 완료

- [x] 🔴 BUG-01 `core/pipeline.py:41,67` — 체크포인트에서 state를 복구하지만 `from_step`이 자동으로 결정되지 않아 항상 STEP 1부터 재실행됨 → `run(from_step=0)` 기본값 변경 + `last_completed_step()+1` 자동 계산으로 수정, `main.py` `--step` 기본값도 0으로 변경
- [x] 🔴 BUG-02 `core/pipeline.py:_step7_bgm` — `scenario_ko`가 비어있을 경우 `stage_counts`가 `{}` → `max()` `ValueError` 발생 → `stage_counts` 빈 경우 `"core"` 기본값 사용으로 수정
- [x] 🟡 BUG-03 `core/pipeline.py:_cli_confirm` — 빈 엔터 입력이 `True`(확인)로 처리되어 실수로 컨펌 가능 → `""` 를 허용 목록에서 제거
- [ ] 🔵 BUG-04 `core/pipeline.py:_step8_compose` — shorts 합성 시 landscape 전체 `audio_paths` 전달. shorts 시나리오는 일부 scene_id만 포함하므로 인덱스 기반 매핑 불일치 가능 → 대화 6(영상 합성)에서 `compose_shorts` 구현 시 `scene_id` 기반 오디오 필터링으로 해결 예정

---

**대화 9 — 부가 기능** ✅ 체크 완료

- [x] 🟡 BUG-34 `scheduler/cron_runner.py:14` — `import time` 미사용 (`_stop_event.wait()` 사용으로 불필요) → 즉시 제거
- [x] 🟡 BUG-35 `history/history_manager.py:13` — `from pathlib import Path` 미사용 → 즉시 제거
- [x] 🟡 BUG-36 `analytics/cost_reporter.py:190` — `total_generated = total_images + total_reused` 이중 계산 오류. `image_count`가 이미 재사용 이미지를 포함하므로 `total_generated = total_images`로 수정
- [ ] 🔵 BUG-37 `analytics/cost_reporter.py:95,180,220` — `get_all(limit=10000)`으로 전체 기록을 파이썬에서 집계. 대량 데이터 시 비효율적이나 현재 규모에서는 무해. 대화 12 통합 테스트 후 DB 집계 쿼리로 개선 예정
- [ ] 🔵 BUG-38 `analytics/youtube_analytics.py:29-57` — `_get_analytics_service()`가 업로드 전용 OAuth 토큰으로 Analytics API 서비스 생성 시도. `yt-analytics.readonly` 스코프 없이는 API 호출 실패. None 반환 후 graceful 스킵 처리됨 (설계 의도)
- [ ] 🔵 BUG-39 `scheduler/cron_runner.py:97` — `_repeat_days` 등 상태 변수가 스레드 간 lock 없이 공유됨. CPython GIL로 실질적 크래시 없으나 이론상 race condition 존재
- [ ] 🔵 BUG-40 `history/history_manager.py:91` — `add_from_pipeline()`에서 `reused_images`가 0으로 고정. pipeline에서 이미지 재사용 수를 추적하지 않아 수동 `update_reused_images()` 호출 필요. 대화 12 통합 테스트 시 pipeline 연동 개선 예정

---

**대화 10 — GUI** ✅ 체크 완료

- [x] 🟡 BUG-41 `gui/tabs/main_tab.py:172` — `QThread.terminate()` 사용. 리소스 누수/크래시 가능 → `request_stop()` + `_stop_requested` 플래그 방식으로 안전한 중단으로 변경. 컨펌 대기 중이면 거부로 처리하여 PipelineAborted 발생시킴
- [x] 🔴 BUG-42 `gui/tabs/main_tab.py:194` + `gui/app.py:63` — 파이프라인 완료 후 ResultTab에 state 미전달. `show_result()` 메서드 존재하나 호출되지 않음 → `PipelineWorker.finished` 시그널에 state dict 추가, `MainTab.pipeline_finished` 시그널 추가, `MainWindow._setup_ui()`에서 `pipeline_finished → show_result` 연결
- [ ] 🟡 BUG-43 `gui/tabs/result_tab.py:131` — `_on_upload()`가 GUI 스레드에서 YouTube API 호출. 4개 영상 업로드 중 UI 프리즈 발생. 대화 12 통합 테스트 시 QThread 워커로 분리 예정
- [ ] 🟡 BUG-44 `gui/tabs/analytics_tab.py:132` — `_refresh_performance()`가 GUI 스레드에서 YouTube API 다중 호출. 최대 20개 레코드 × 4영상 = 80회 API 호출로 장시간 UI 프리즈 가능. 대화 12 통합 테스트 시 백그라운드 워커로 분리 예정
- [x] 🔵 BUG-45 `gui/components/confirm_dialog.py:35` — `data: dict = None` mutable default argument → `Optional[dict] = None`으로 타입 힌트 명확화

---

**대화 11 — 패키징** ✅ 체크 완료

- [x] 🔴 `build.py` — `os` import가 `main()` 내부에만 있어 `build_onefile()`에서 `os.pathsep` 사용 시 `NameError` 발생 → 모듈 레벨로 import 이동하여 즉시 수정
- [ ] 🔵 BUG-46 `webpage-to-youtube.spec:30` — `datas`에 glob 패턴 `*.txt` 사용. PyInstaller 일부 버전에서 glob 미지원 시 빈 리스트 반환 가능. 통합 테스트 시 실제 빌드로 검증 필요
- [x] 🟡 BUG-47 `config.py:15` — PyInstaller frozen 모드에서 `BASE_DIR = Path(__file__).parent`가 임시 디렉토리를 가리킴 → `sys.frozen` 감지하여 번들 리소스(`ASSETS_DIR`, `PROMPTS_DIR`)는 `sys._MEIPASS`, 사용자 데이터(`OUTPUT_DIR`, `DATABASE_DIR`)는 `sys.executable` 기준으로 분기 추가

---

**대화 12 — 통합 테스트** ✅ 체크 완료

- [x] 🔴 BUG-48 `pipeline.py:177` — `from image.generator.dalle_generator import generate_images` 함수명 불일치. 실제: `generate_image`. `batch_processor.generate_all()`이 엔진 선택을 내부 처리하므로 → `_step5_images()`를 `generate_all()` 직접 호출로 변경
- [x] 🔴 BUG-49 `pipeline.py:179` — `from image.generator.sd_generator import generate_images` 동일 이슈 → BUG-48과 함께 수정
- [x] 🔴 BUG-50 `pipeline.py:181` — `from image.batch_processor import batch_generate` 함수 미존재. 실제: `generate_all`. 파라미터 인터페이스도 완전히 다름 → `generate_all(scenes, output_dir, cost_tracker)` 호출로 변경
- [x] 🔴 BUG-51 `pipeline.py:288` — `from thumbnail.prompt_generator import generate_thumb_prompt` 함수명 불일치. 실제: `generate_thumbnail_prompt(title_ko, title_en, scenes, cost_tracker)` → 올바른 함수명과 파라미터로 변경
- [x] 🔴 BUG-52 `pipeline.py:289` — `from thumbnail.image_generator import generate_thumb_image` 함수명 불일치. 실제: `generate_thumbnail_image`. 상위 래퍼 `generate_both_base_images` 사용으로 변경
- [x] 🔴 BUG-53 `pipeline.py:290` — `from thumbnail.text_overlay import overlay_text` 함수명 불일치. 실제: `apply_text_overlay`. 상위 래퍼 `generate_all_thumbnails` 사용으로 4종 썸네일 일괄 생성

---

## 13. 실행 방법

```
개발/디버깅 시   python main.py
일반 사용 시     webpage-to-youtube.exe (Windows)
                 webpage-to-youtube.app (Mac)
빌드 방법       python build.py              (기본 onedir)
                python build.py --onefile    (단일 exe)
                python build.py --clean      (정리 후 빌드)
                python build.py --debug      (콘솔 포함 디버그)
```

최초 실행 시 설정 탭에서 API 키 입력 후 저장하면 바로 사용 가능.
